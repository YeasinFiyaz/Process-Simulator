# clone
git clone https://github.com/<your-username>/process-simulator.git
cd process-simulator

# (optional) create a venv
python -m venv .venv && source .venv/bin/activate  # Windows: .venv\Scripts\activate

# install CLI
pip install -e .
