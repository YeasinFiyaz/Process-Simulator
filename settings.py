# procsim/procsim/settings.py
INSTALLED_APPS = [
    # ...
    "sim",
]

# Keep default TEMPLATES, but ensure APP_DIRS=True (it is by default)
# STATIC_URL is fine as default; you can add STATICFILES_DIRS later if needed
